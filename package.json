{
  "name": "@koreanwglasses/mutable",
  "version": "0.0.0-alpha.0",
  "packageManager": "yarn@3.2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepare": "$npm_execpath run build",
    "build": "rm -rf dist && tsc"
  },
  "devDependencies": {
    "@types/events": "^3.0.0",
    "@types/node": "^17.0.32",
    "tsc": "^2.0.4",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "events": "^3.3.0",
    "tiny-typed-emitter": "^2.1.0"
  }
}
